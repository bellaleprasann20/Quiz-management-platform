# Application Settings
PROJECT_NAME="Quiz Management Platform"

# Database Configuration (Defaults to SQLite, change to PostgreSQL in production)
DATABASE_URL="sqlite:///./quiz.db"

# Security (Generate a real secret key for production!)
SECRET_KEY="your-super-secret-key-change-this-later"
ACCESS_TOKEN_EXPIRE_MINUTES=1440